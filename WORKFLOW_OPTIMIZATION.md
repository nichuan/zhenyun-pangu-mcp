# Skill / MCP 协作优化（2026-09-16）

本轮围绕现有 10 个技能与 MCP 源码完成优化。重点是让相同任务在不同 agent 中复用同一套边界和证据，保留已有局部修改的快速路径。未调用生产业务接口，未修改线上数据，也未替换已安装的插件缓存。

## 已发现的问题与修改

| 问题 | 影响 | 已落地修改 |
| --- | --- | --- |
| 履约 SQL 和 Archery 将目录/模板字段视为当前事实，寻源 SQL 却要求实时校验 | 跨技能后证据可信标准变化，容易把旧字段用到另一环境 | 统一候选与实时证据；目标环境结构可复用，切环境/升级/冲突才重新核实 |
| 路由只说明“传递上下文”，未定义证据适用范围与验证阶段 | 接手 agent 重复查询或复用过期状态 | 增加版本化交接示例：目标、范围、证据引用/时间/版本、结论、验收、授权引用和下一步 |
| 需求、诊断、修复结束条件互不衔接 | 用户要求全程处理时可能停在中间；本地完成与平台验证混淆 | 增加验收条件到产物/证据/检查的对应规则；用户已要求后续工作时继续，明确未运行的验证 |
| 版本号仅递增、WHERE 仅主键+租户 | 可能覆盖调查后发生的并发修改 | 核查与修复条件一致，加入已读取旧状态/版本；截断结果不能证明影响范围；执行后校验必需 |
| 只校验字段即可将修复模板标 verified | 未验证的修复逻辑被当成已成功经验 | 结构、适用前提、效果分开；仅生成 SQL 的方案保持 draft，记录已核验部分 |
| 自动询问沉淀与“按价值沉淀”规则并存 | 常规任务多出确认轮次 | 只为可复用新结论准备草稿；沿用同范围明确授权，按需查重再保存 |
| MCP 未声明工具读写属性 | 客户端无法通过协议判断查询、统计、评论、知识更新的差别 | 全工具显式 ToolAnnotations；未知新工具未分类时拒绝注册；写工具保守标记非幂等 |
| 主要分发流程绑定个人 Codex 插件路径 | 换客户端需要重做接入 | 增加通用 stdio MCP + skills 导出；凭据外置；无本地技能客户端可通过工具读取简要指引 |

## 接入与维护

安装支持 Python >=3.10 的环境与 `uv`，从 MCP 项目目录执行（目标目录必须不存在）：

```bash
python scripts/export_agent_bundle.py \
  --output /path/to/new-pangu-agent-bundle \
  --env-dir /path/to/private-pangu-config
```

生成 `mcp.json`、`AGENTS.md`、`skills/` 和 `server/`。将 `mcpServers` 条目导入目标客户端的 MCP 配置，按该客户端方式加载 `skills/*/SKILL.md`。凭据由外部 `MCP_ENV_DIR/.env` 提供；模板见 `server/.env.example`。导出脚本不读取或复制原有 `.env`、Git 目录、虚拟环境或缓存。迁移到另一机器后修改配置中的两个绝对目录路径。

能力分级：支持本地技能的客户端加载完整领域流程；仅支持 MCP 工具的客户端可调用 `get_workflow_guide` 获取协作摘要；没有子代理/并行调用也可按依赖串行完成。摘要不替代完整领域规则，不能保证每个客户端自动发现技能或展示注解的方式相同。

协作协议唯一源为 `src/zhenyun_pangu_mcp/workflow_guide.md`；发布时同步至 `../custom-skills/zhenyun-ops/references/collaboration-contract.md`，契约校验会拒绝内容漂移。各技能仅在跨流程/恢复时加载。工具 schema 仍来自实际 `tools/list`，`capabilities` 不进行后端健康探测。

```bash
uv run --frozen pytest -q
uv run --frozen python scripts/validate_skill_mcp_contracts.py
```

运行时副作用分类在 `tool_policy.py` 显式维护。新增工具必须分类，并在某个 `skill.json` 声明归属。注解帮助客户端决策，不构成用户授权；业务数据库只读仍由服务端查询校验约束。现有工具参数与返回格式保持不变，新工具为增量能力。SDK 下界对齐本轮实际验证的 MCP 1.29.0，锁文件已固定该版本。

## 验证范围

- 原有 159 项测试通过；新增后总计 171 项通过。
- 新增真实 stdio 客户端测试：初始化、tools/list、读取交接指引、拒绝非法 topic；无需业务凭据。
- 检查客户端可见注解、动态工具清单、未知工具拒绝注册、导出排除凭据/缓存、拒绝覆盖与符号链接。
- Skill/MCP 契约检查覆盖 10 个技能、60 个默认工具，以及协作协议副本、技能本地链接与工具注解。
- 10 个 SKILL.md 均通过 skill-creator 格式校验；通用导出包验证通过；sdist/wheel 构建成功，从 wheel 内可读取全部 6 个协作主题。
- 尚未逐一实测各厂商 agent 的发现/展示行为，未进行生产修复或平台发布测试。

## 后续建议（按收益排序）

1. **建立真实任务回归集**：选择局部脚本修改、新二开需求、工作台问题、诊断转修复、知识库不可用、跨环境恢复各 2–3 个脱敏样本。记录成功率、无效工具轮次、重复询问、错误环境查询和“未验证却下结论”的次数。固定输入与验收，比较不同 agent 的实际表现。
2. **统一 MCP 结果协议**：当前知识工具仍返回 Markdown，其它工具多为 JSON。后续增加兼容旧正文的结构化结果（状态、证据范围、截断/完整性、错误分类），分批迁移；不要一次改变全部返回值破坏现有客户端。
3. **区分写入授权与重试机制**：知识维护/评论目前靠工具说明与 agent 遵守授权。若需面向更多使用者，设计服务端权限配置、写入幂等键与审计记录；写入不确定时先查结果，避免重复评论/知识条目。
4. **标准 Java 交付能力独立建设**：现有需求技能限定 Marmot。本轮补齐标准/混合需求的分工入口，但未把它扩成标准仓库构建、集成测试、发布流程。应结合真实仓库约定与样本单独补齐。
5. **做按需健康诊断与证据缓存**：明确配置缺失/鉴权失败/空结果的差异；只有首次接入或故障时探测。先测量收益再引入缓存，按环境、租户、版本、查询窗口分区，可变状态不持久复用。

本轮先解决已证实的规则冲突和接口可发现性；上面这些更大范围的行为变化保留为后续独立迭代，避免把每个任务都变成复杂流程。
