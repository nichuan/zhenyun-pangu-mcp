# Agent 性能基准

速度问题分成三段记录，避免只比较最终体感：

1. **模型阶段**：首条可见更新时间、每次工具结果后的决策时间。
2. **编排阶段**：工具总调用数、必须串行的最长依赖链、可并行但被串行的调用数。
3. **MCP 阶段**：每个工具的服务端耗时、返回字符数、超时和重试次数。

## 只读 MCP 基准

在 `zhenyun-pangu-mcp` 目录执行：

```bash
UV_CACHE_DIR=/private/tmp/zhenyun-uv-cache uv run python scripts/benchmark_read_paths.py --repeat 3
```

脚本只访问认知层的只读接口，只输出耗时、返回长度和失败次数，不打印业务数据或凭据。`template_keyword` 与 `template_hybrid` 的差值用于判断语义向量检索是否值得进入默认路径；`search_pangu` 与 `diagnose_context` 用于观察组合检索并发效果。

## Agent A/B 场景

使用完全相同的输入、环境、模型推理档位和网络条件，各运行至少三次并取中位数：

| 场景 | 期望路径 | 重点指标 |
| --- | --- | --- |
| 已有 Marmot 脚本改一个字段 | 本地读取 → 修改 → 并行语法/静态检查 | 远程调用数应为 0 |
| 带 traceId 的二开异常 | 一次定向日志 → 必要时一轮源码/数据证据 | 串行工具深度、是否证据足够即停 |
| 指定环境/租户/单号的数据修复 | 并行模板关键词/租户/表发现 → 有界 JOIN/依赖查询 → SQL | 是否跳过重复 describe 与语义检索 |

每次记录：总耗时、首条可见更新时间、模型等待、MCP 等待、工具调用数、串行深度、输出是否正确。没有这些数据时，只能判断当前实现中的结构性慢点，不能断言 Codex 与其它 Agent 的纯模型速度差异。
